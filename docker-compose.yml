services:
  rag_feeder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag_feeder_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - rag-network

networks:
  rag-network:
    external: true